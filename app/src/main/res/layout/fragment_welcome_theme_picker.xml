<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"

    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/themePickerRadioGroup"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/themePickerContinueButton"
        android:layout_alignParentTop="true"
        android:layout_centerInParent="true"
        android:layout_margin="@dimen/largeOffset"
        android:orientation="vertical">

        <FrameLayout
            android:id="@+id/themePickerLightBlock"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1">

            <RadioButton
                android:id="@+id/themePickerLightThemeRadioButton"
                android:text="@string/themePickerLight"
                style="@style/themePickerRadioButton"
                />

        </FrameLayout>

        <FrameLayout
            android:id="@+id/themePickerDarkBlock"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1">

            <RadioButton
                android:id="@+id/themePickerDarkThemeRadioButton"
                android:text="@string/themePickerDark"
                style="@style/themePickerRadioButton"
                />

        </FrameLayout>

    </LinearLayout>

    <Button
        android:id="@+id/themePickerContinueButton"
        style="@style/buttonContinue"
        android:theme="@style/coloredAppCompatButton"/>

</RelativeLayout>